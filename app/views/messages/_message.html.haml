.Chat-main
  .Chat-main__group_info
    .Chat-main__group_info__message
      = message.user.name
    .MessageInfo__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .Chat-main__content
    - if message.content.present?
      %p.Message__content
        = message.content
    = image_tag message.image.url, class: 'Message__image' if message.image.present?